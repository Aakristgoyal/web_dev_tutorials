# why  use React ?
- we can use states which means that once we update the state variable it changes automatically across the different pages.

- we can split our app into multiple components and reuse them and can modify them easily.

- we can use react to efficiently update the ui which is better than updating it using dom manipulation.

- Debugging and maintainance is easy.